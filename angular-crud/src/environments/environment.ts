// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyCVJdcUtJFjbQefKcWIbFmd2kACjlOMr8Y",
    authDomain: "fb-contactmanager.firebaseapp.com",
    databaseURL: "https://fb-contactmanager.firebaseio.com",
    projectId: "fb-contactmanager",
    storageBucket: "fb-contactmanager.appspot.com",
    messagingSenderId: "379523582053",
    appId: "1:379523582053:web:3303b4993ec0eece"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
